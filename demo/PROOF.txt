# OddSockets Elixir SDK - live two-client round-trip PROOF
#
# Captured 2026-07-22T01:18:39Z against the LIVE OddSockets platform.
# Built and run straight from the SDK sources via demo/Dockerfile:
#
#   docker build -f demo/Dockerfile -t oddsockets-elixir-demo .
#   docker run --rm -e ODDSOCKETS_API_KEY=ak_your_key_here oddsockets-elixir-demo
#
# TWO independent clients (subscriber alice + publisher bob) on separate
# connections. alice receiving bob's uniquely-nonced message can ONLY have
# travelled through the OddSockets worker - it is not a local echo. API key
# is redacted below; everything else is the verbatim run.
#
# Transport: genuine Socket.IO (Engine.IO v4) over WebSocket (WebSockex) to the
# worker; request/response events correlated per channel.
#
# Process exit code: 0 (0 = cross-client round-trip verified)

[connect] connecting both clients...
[alice] worker w002-oddsockets-1
[bob]   worker w002-oddsockets-1
[connect] alice = connected, bob = connected
[alice] subscribed to demo-610597 (presence on)
[bob] published, messageId = 36401ebb-c78a-4617-aa0c-1cf45cdfc403
[alice] received bob's message (nonce matched) - real round-trip.
[alice] presence: 1 user(s).
[alice] unsubscribed.
OK - cross-client round-trip verified


# OddSockets Elixir SDK - enhanced-events receive-path PROOF
#
# Captured 2026-07-23T11:40:00Z against the LIVE OddSockets platform.
#   docker build -f demo/Dockerfile -t oddsockets-elixir-enh .
#   docker run --rm -e ODDSOCKETS_API_KEY=ak_... oddsockets-elixir-enh \
#     mix run -e "OddSocketsEnhancedRegress.run()"
#
# bob fires enhanced (Slack-like) actions; because alice is on a SEPARATE
# connection, the broadcasts reaching alice's public event stream can only have
# travelled through the worker. No mocks, no local echo.
#
# Process exit code: 0

[connect] connecting both clients...
[alice] worker w002-oddsockets-1
[bob]   worker w002-oddsockets-1
[connect] alice = connected, bob = connected
[both] subscribed to enh-b4b0f1d4a0
[bob] EnhancedFeatures.start_typing(bob) ...
[bob] published messageId=070c4920-2100-4832-a1d9-e3a1aba75625, EnhancedFeatures.add_reaction :thumbsup: ...
[alice] received 'user_typing' from bob (channel enh-b4b0f1d4a0) - broadcast round-trip.
[alice] received 'reaction_added' (:thumbsup:) from bob - broadcast round-trip.
OK - enhanced broadcast receive-path verified (user_typing + reaction_added)
